// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("evenement", "for evenement support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("evenement", "enable evenement support", "no");

if (PHP_EVENEMENT != "no") {
	EXTENSION("evenement", "evenement.c");
}

